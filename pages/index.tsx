import React from 'react'
import type { NextPage } from 'next'
import {useState, useEffect} from 'react'
import Head from 'next/head'
import MagneticButton from '../components/MagneticButton'
import CursorFollower from '../components/CursorFollower'
import FollowCursor from '../components/FollowCursor'
import Script from 'next/script'
import Header from '../components/Header'
import ParallaxItem from '../components/ParallaxItem'

const color1 = '#000'
const color2 = '#eee'
 
const Home: NextPage = () => {

  const [isDark, setDark] = useState(true)

  const [posX, setPosX] = useState(0)
  const [posY, setPosY] = useState(0)

  useEffect(() => {
    const setFromEvent = (e: { clientX: any; clientY: any }) => {
      setTimeout(() => {
        setPosX(e.clientX)
        setPosY(e.clientY)
      }, 100);
    }
    window.addEventListener("mousemove", setFromEvent);
    return () => {
      window.removeEventListener("mousemove", setFromEvent);
    };
  }, []);

  return (
    <>
      <Head>
        <title>Studio</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>

      <main  style={{backgroundColor:isDark?color1:color2, color:isDark?color2:color1}}>
      <CursorFollower/>
        <div data-scroll >
          <div className="content content--full content--alternate">
            <ParallaxItem no="01" title="Oh" imgURL='url(img/5.jpg)'
              description="Little trees and bushes grow however makes them happy"
            />
            <ParallaxItem no="02" title="Ri" imgURL='url(img/6.jpg)'
              description="We don't have to be committed. We are just playing here."
            />
            <ParallaxItem no="03" title="Nj" imgURL='url(img/5.jpg)'
              description="I thought today we would do a happy little picture."
            />
            <ParallaxItem no="04" title="abc" imgURL='url(img/6.jpg)'
              description="I thought today we would do a happy little picture."
            />
            <ParallaxItem no="05" title="def" imgURL='url(img/5.jpg)'
              description="I thought today we would do a happy little picture."
            />
          </div>
          
        </div>
          <Header/>
          <MagneticButton isDark={isDark} changeSetDark={setDark}/>
      </main>
      
      
      <Script src="./js/demo4.js"></Script>      
      <Script src="./js/imagesloaded.pkgd.min.js"></Script>
      <Script src="./js/scrolling.js"></Script>
    </>
  )
}

export default Home